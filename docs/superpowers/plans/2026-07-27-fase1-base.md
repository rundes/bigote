# Fase 1 — Base: proyecto, esquema+RLS, auth, shell multi-org

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** App Next.js + Supabase desplegable con esquema completo (RLS incluida), login Google/password/magic link, shell multi-organización con switcher y navegación por permisos, y panel de plataforma para super-admins.

**Architecture:** Next.js App Router con rutas por organización (`/o/[orgId]/...`); Supabase como única fuente de datos con RLS por membresía; helpers SQL `security definer` para chequear permisos; migraciones versionadas con Supabase CLI aplicadas al proyecto hosted de dev (ref `olmjkuapainklekdzntk`, sin Docker); seed idempotente por script Node con el admin client; tests de RLS con Vitest contra ese proyecto.

**Tech Stack:** Next.js 15 (App Router, TS estricto), Tailwind CSS v4, @supabase/supabase-js v2 + @supabase/ssr, Supabase CLI, Vitest, Lucide React, fuente Figtree.

## Global Constraints

- TypeScript estricto, sin `any` injustificados; ESLint sin errores (spec §8).
- Toda la UI en español rioplatense voseo: "Creá", "Continuá con Google", "Tomala" (spec §2).
- Moneda ARS `es-AR`; TZ `America/Argentina/Buenos_Aires` (spec §2).
- Responsive usable en 380 px; targets táctiles ≥44 px (PRODUCT.md).
- Tokens visuales de `DESIGN.md`: fondo papel cálido, acento terracota, Figtree, nada de #fff/#000 ni azul-admin.
- Nunca exponer `SUPABASE_SECRET_KEY` al cliente; solo en server actions/route handlers.
- Commits frecuentes, mensajes en español, footer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Scaffold Next.js + tokens de diseño

**Files:**
- Create: proyecto Next.js en la raíz del repo (`app/`, `package.json`, etc.)
- Modify: `app/globals.css`, `app/layout.tsx`, `README.md`

**Interfaces:**
- Produces: clases Tailwind con tokens (`bg-fondo`, `text-tinta`, `text-tinta-suave`, `bg-panel`, `bg-superficie`, `border-linea`, `bg-acento`, `text-acento`, `text-ok`, `text-peligro`), fuente Figtree como default.

- [ ] **Step 1: Scaffold**

```bash
cd C:/Users/rundes/bigote
npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*" --use-npm --turbopack
npm install lucide-react
```

(Si `create-next-app` se queja de directorio no vacío por `docs/`/`PRODUCT.md`: scaffoldear en `tmp-scaffold/`, mover el contenido a la raíz sin pisar los docs, borrar `tmp-scaffold/`.)

- [ ] **Step 2: Tokens en `app/globals.css`** (Tailwind v4, reemplazar contenido)

```css
@import "tailwindcss";

@theme {
  --color-fondo: oklch(0.97 0.008 75);
  --color-panel: oklch(0.94 0.01 75);
  --color-superficie: oklch(0.99 0.005 75);
  --color-tinta: oklch(0.25 0.015 60);
  --color-tinta-suave: oklch(0.48 0.02 60);
  --color-linea: oklch(0.88 0.012 75);
  --color-acento: oklch(0.58 0.13 45);
  --color-acento-tinta: oklch(0.99 0.005 75);
  --color-ok: oklch(0.55 0.12 150);
  --color-peligro: oklch(0.55 0.16 25);
  --color-aviso: oklch(0.7 0.12 85);
  --font-sans: var(--font-figtree), system-ui, sans-serif;
}

body {
  background: var(--color-fondo);
  color: var(--color-tinta);
}
```

- [ ] **Step 3: `app/layout.tsx` con Figtree y metadata**

```tsx
import type { Metadata } from "next";
import { Figtree } from "next/font/google";
import "./globals.css";

const figtree = Figtree({ subsets: ["latin"], variable: "--font-figtree" });

export const metadata: Metadata = {
  title: "bigote",
  description: "Gestión de proyectos, finanzas y espacios para tu organización",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="es-AR">
      <body className={`${figtree.variable} font-sans antialiased`}>{children}</body>
    </html>
  );
}
```

- [ ] **Step 4: Verificar build**

Run: `npm run build && npm run lint`
Expected: build OK, lint sin errores.

- [ ] **Step 5: README mínimo** (secciones: qué es, setup local `npm install` + `.env.local` + `supabase start`, comandos; se completa en tareas siguientes) y **commit**

```bash
git add -A && git commit -m "feat: scaffold Next.js 15 con tokens de diseño y Figtree"
```

---

### Task 2: Supabase CLI + clientes

**Files:**
- Create: `supabase/config.toml` (via CLI), `lib/supabase/client.ts`, `lib/supabase/server.ts`, `lib/supabase/admin.ts`, `.env.example`
- Modify: `.gitignore` (`.env*.local`, `supabase/.temp`)

**Interfaces:**
- Produces: `crearClienteNavegador(): SupabaseClient`, `crearClienteServidor(): Promise<SupabaseClient>` (cookies de Next), `crearClienteAdmin(): SupabaseClient` (service role, solo server).

- [ ] **Step 1: Init + deps**

```bash
npm install @supabase/supabase-js @supabase/ssr
npx supabase init
```

- [ ] **Step 2: `.env.example`**

```
NEXT_PUBLIC_SUPABASE_URL=https://<ref>.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<publishable key, sb_publishable_...>
SUPABASE_SECRET_KEY=<secret key sb_secret_..., NUNCA al cliente>
```

- [ ] **Step 3: `lib/supabase/client.ts`**

```ts
import { createBrowserClient } from "@supabase/ssr";

export function crearClienteNavegador() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!
  );
}
```

- [ ] **Step 4: `lib/supabase/server.ts`**

```ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";

export async function crearClienteServidor() {
  const cookieStore = await cookies();
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (all) => {
          try {
            all.forEach(({ name, value, options }) => cookieStore.set(name, value, options));
          } catch {
            // llamado desde un Server Component: el middleware refresca la sesión
          }
        },
      },
    }
  );
}
```

- [ ] **Step 5: `lib/supabase/admin.ts`**

```ts
import "server-only";
import { createClient } from "@supabase/supabase-js";

export function crearClienteAdmin() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SECRET_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } }
  );
}
```

(`npm install server-only`.)

- [ ] **Step 6: Build + commit**

Run: `npm run build`
```bash
git add -A && git commit -m "feat: Supabase CLI y clientes navegador/servidor/admin"
```

---

### Task 3: Migración 0001 — esquema completo

**Files:**
- Create: `supabase/migrations/0001_esquema.sql`

**Interfaces:**
- Produces: todas las tablas del spec §3 con esos nombres exactos; trigger `crear_perfil` en `auth.users`; constraint anti-solape en `reservas`.

- [ ] **Step 1: Escribir la migración**

```sql
create extension if not exists btree_gist;

create table perfiles (
  id uuid primary key references auth.users (id) on delete cascade,
  nombre text not null default '',
  email text not null,
  created_at timestamptz not null default now()
);

create table super_admins (
  perfil_id uuid primary key references perfiles (id) on delete cascade
);

create table organizaciones (
  id uuid primary key default gen_random_uuid(),
  nombre text not null,
  tipo text not null default 'empresa' check (tipo in ('empresa', 'asociacion_civil', 'otro')),
  created_at timestamptz not null default now()
);

create table roles (
  id uuid primary key default gen_random_uuid(),
  org_id uuid not null references organizaciones (id) on delete cascade,
  nombre text not null,
  permisos jsonb not null default '{"proyectos":false,"equipo":false,"finanzas":false,"espacios":false,"admin":false}',
  unique (org_id, nombre)
);

create table membresias (
  org_id uuid not null references organizaciones (id) on delete cascade,
  perfil_id uuid not null references perfiles (id) on delete cascade,
  rol_id uuid not null references roles (id),
  activo boolean not null default true,
  created_at timestamptz not null default now(),
  primary key (org_id, perfil_id)
);

create table proyectos (
  id uuid primary key default gen_random_uuid(),
  org_id uuid not null references organizaciones (id) on delete cascade,
  nombre text not null,
  estado text not null default 'activo' check (estado in ('activo', 'archivado')),
  creado_por uuid references perfiles (id),
  created_at timestamptz not null default now()
);

create table proyecto_miembros (
  proyecto_id uuid not null references proyectos (id) on delete cascade,
  perfil_id uuid not null references perfiles (id) on delete cascade,
  primary key (proyecto_id, perfil_id)
);

create table tareas (
  id uuid primary key default gen_random_uuid(),
  proyecto_id uuid not null references proyectos (id) on delete cascade,
  titulo text not null,
  descripcion text not null default '',
  dificultad int not null check (dificultad between 1 and 5),
  asignado_a uuid references perfiles (id),
  estado text not null default 'pendiente' check (estado in ('pendiente', 'en_curso', 'hecha')),
  completada_por uuid references perfiles (id),
  completada_at timestamptz,
  created_at timestamptz not null default now()
);

create table clientes (
  id uuid primary key default gen_random_uuid(),
  org_id uuid not null references organizaciones (id) on delete cascade,
  nombre text not null,
  contacto text,
  created_at timestamptz not null default now()
);

create table edificios (
  id uuid primary key default gen_random_uuid(),
  org_propietaria_id uuid not null references organizaciones (id) on delete cascade,
  org_gestora_id uuid references organizaciones (id),
  nombre text not null,
  direccion text not null default '',
  descripcion text not null default '',
  destino_ingresos text not null default 'propietaria'
    check (destino_ingresos in ('propietaria', 'gestora', 'reparto')),
  porcentaje_propietaria numeric,
  created_at timestamptz not null default now(),
  check (destino_ingresos <> 'reparto' or porcentaje_propietaria between 0 and 100),
  check (destino_ingresos = 'propietaria' or org_gestora_id is not null)
);

create table salas (
  id uuid primary key default gen_random_uuid(),
  edificio_id uuid not null references edificios (id) on delete cascade,
  nombre text not null,
  tipo text not null default 'publica' check (tipo in ('publica', 'privada')),
  descripcion text not null default '',
  activa boolean not null default true,
  created_at timestamptz not null default now()
);

create table espacio_media (
  id uuid primary key default gen_random_uuid(),
  edificio_id uuid references edificios (id) on delete cascade,
  sala_id uuid references salas (id) on delete cascade,
  tipo text not null check (tipo in ('foto', 'video')),
  storage_path text not null,
  orden int not null default 0,
  check ((edificio_id is null) <> (sala_id is null))
);

create table planes_reserva (
  id uuid primary key default gen_random_uuid(),
  org_id uuid not null references organizaciones (id) on delete cascade,
  nombre text not null,
  gratuito boolean not null default false,
  precio_hora numeric not null default 0,
  horas_gratis_mes int,
  solo_salas_publicas boolean not null default false
);

create table reservas (
  id uuid primary key default gen_random_uuid(),
  sala_id uuid not null references salas (id),
  plan_id uuid not null references planes_reserva (id),
  cliente_id uuid references clientes (id),
  para_perfil_id uuid references perfiles (id),
  fecha date not null,
  hora_inicio int not null check (hora_inicio between 8 and 21),
  horas int not null check (horas between 1 and 14),
  costo numeric not null default 0,
  estado text not null default 'confirmada' check (estado in ('confirmada', 'cancelada')),
  motivo_cancelacion text,
  creada_por uuid not null references perfiles (id),
  created_at timestamptz not null default now(),
  check ((cliente_id is null) <> (para_perfil_id is null)),
  constraint reservas_sin_solape exclude using gist (
    sala_id with =,
    fecha with =,
    int4range(hora_inicio, hora_inicio + horas) with &&
  ) where (estado = 'confirmada')
);

create table movimientos (
  id uuid primary key default gen_random_uuid(),
  org_id uuid not null references organizaciones (id) on delete cascade,
  edificio_id uuid references edificios (id),
  tipo text not null check (tipo in ('ingreso', 'egreso')),
  categoria text not null default 'general',
  monto numeric not null check (monto > 0),
  detalle text not null default '',
  fecha date not null default (now() at time zone 'America/Argentina/Buenos_Aires')::date,
  origen text not null default 'manual' check (origen in ('manual', 'reserva')),
  reserva_id uuid references reservas (id),
  creado_por uuid references perfiles (id)
);

create or replace function public.crear_perfil()
returns trigger language plpgsql security definer set search_path = public as $$
begin
  insert into public.perfiles (id, nombre, email)
  values (new.id, coalesce(new.raw_user_meta_data ->> 'name', ''), new.email)
  on conflict (id) do nothing;
  return new;
end $$;

create trigger crear_perfil_trigger
  after insert on auth.users
  for each row execute function public.crear_perfil();
```

- [ ] **Step 2: Aplicar en el proyecto hosted (sin Docker)**

El controlador aplica la migración vía MCP de Supabase (`apply_migration`, proyecto `olmjkuapainklekdzntk`); avisarle cuando el archivo esté listo. Alternativa CLI: `npx supabase link --project-ref olmjkuapainklekdzntk && npx supabase db push`.
Expected: migración aplicada sin errores.

- [ ] **Step 3: Probar el constraint anti-solape a mano**

Vía SQL en el proyecto (MCP `execute_sql`): insertar org+rol+perfil dummy, dos reservas solapadas → la segunda debe fallar con `conflicting key value violates exclusion constraint "reservas_sin_solape"`. Borrar los datos dummy al final. (Prueba manual rápida; el test automatizado va en Task 8.)

- [ ] **Step 4: Commit**

```bash
git add supabase && git commit -m "feat: migración 0001, esquema completo con anti-solape y trigger de perfiles"
```

---

### Task 4: Migración 0002 — RLS

**Files:**
- Create: `supabase/migrations/0002_rls.sql`

**Interfaces:**
- Produces: funciones SQL `es_super_admin()`, `es_miembro(org uuid)`, `tiene_permiso(org uuid, permiso text)`, `opera_edificio(edificio uuid)`; RLS activa en todas las tablas.

- [ ] **Step 1: Escribir la migración**

```sql
create or replace function public.es_super_admin()
returns boolean language sql stable security definer set search_path = public as $$
  select exists (select 1 from super_admins where perfil_id = auth.uid());
$$;

create or replace function public.es_miembro(org uuid)
returns boolean language sql stable security definer set search_path = public as $$
  select exists (
    select 1 from membresias
    where org_id = org and perfil_id = auth.uid() and activo
  );
$$;

create or replace function public.tiene_permiso(org uuid, permiso text)
returns boolean language sql stable security definer set search_path = public as $$
  select exists (
    select 1
    from membresias m join roles r on r.id = m.rol_id
    where m.org_id = org and m.perfil_id = auth.uid() and m.activo
      and coalesce((r.permisos ->> permiso)::boolean, false)
  );
$$;

create or replace function public.opera_edificio(edificio uuid)
returns boolean language sql stable security definer set search_path = public as $$
  select exists (
    select 1 from edificios e
    where e.id = edificio
      and (es_miembro(e.org_propietaria_id)
           or (e.org_gestora_id is not null and es_miembro(e.org_gestora_id)))
  );
$$;

alter table perfiles enable row level security;
alter table super_admins enable row level security;
alter table organizaciones enable row level security;
alter table roles enable row level security;
alter table membresias enable row level security;
alter table proyectos enable row level security;
alter table proyecto_miembros enable row level security;
alter table tareas enable row level security;
alter table clientes enable row level security;
alter table edificios enable row level security;
alter table salas enable row level security;
alter table espacio_media enable row level security;
alter table planes_reserva enable row level security;
alter table reservas enable row level security;
alter table movimientos enable row level security;

-- perfiles: el propio siempre; los de compañeros de alguna org compartida
create policy perfiles_select on perfiles for select using (
  id = auth.uid() or es_super_admin() or exists (
    select 1 from membresias m1
    join membresias m2 on m1.org_id = m2.org_id
    where m1.perfil_id = auth.uid() and m1.activo
      and m2.perfil_id = perfiles.id and m2.activo
  )
);
create policy perfiles_update on perfiles for update using (id = auth.uid());

create policy super_admins_select on super_admins for select using (perfil_id = auth.uid());

create policy organizaciones_select on organizaciones for select using (
  es_miembro(id) or es_super_admin()
  or exists (select 1 from edificios e where (e.org_propietaria_id = organizaciones.id or e.org_gestora_id = organizaciones.id) and opera_edificio(e.id))
);
create policy organizaciones_admin on organizaciones for update using (tiene_permiso(id, 'admin'));

create policy roles_select on roles for select using (es_miembro(org_id));
create policy roles_admin on roles for all using (tiene_permiso(org_id, 'admin'));

create policy membresias_select on membresias for select using (
  perfil_id = auth.uid() or es_miembro(org_id)
);
create policy membresias_admin on membresias for all using (tiene_permiso(org_id, 'admin'));

create policy proyectos_select on proyectos for select using (
  tiene_permiso(org_id, 'proyectos') or tiene_permiso(org_id, 'admin')
  or exists (select 1 from proyecto_miembros pm where pm.proyecto_id = proyectos.id and pm.perfil_id = auth.uid())
);
create policy proyectos_write on proyectos for all using (tiene_permiso(org_id, 'proyectos'));

create policy proyecto_miembros_select on proyecto_miembros for select using (
  exists (select 1 from proyectos p where p.id = proyecto_id and es_miembro(p.org_id))
);
create policy proyecto_miembros_write on proyecto_miembros for all using (
  exists (select 1 from proyectos p where p.id = proyecto_id and tiene_permiso(p.org_id, 'proyectos'))
);

-- tareas: solo miembros del proyecto (spec §4); escritura fina (tomar/completar) se refina en fase 2
create policy tareas_select on tareas for select using (
  exists (
    select 1 from proyecto_miembros pm
    where pm.proyecto_id = tareas.proyecto_id and pm.perfil_id = auth.uid()
  ) or exists (
    select 1 from proyectos p where p.id = tareas.proyecto_id and tiene_permiso(p.org_id, 'admin')
  )
);
create policy tareas_write on tareas for all using (
  exists (
    select 1 from proyecto_miembros pm
    where pm.proyecto_id = tareas.proyecto_id and pm.perfil_id = auth.uid()
  ) or exists (
    select 1 from proyectos p where p.id = tareas.proyecto_id and tiene_permiso(p.org_id, 'admin')
  )
);

create policy clientes_select on clientes for select using (es_miembro(org_id));
create policy clientes_insert on clientes for insert with check (es_miembro(org_id));
create policy clientes_admin on clientes for update using (tiene_permiso(org_id, 'espacios'));

create policy edificios_select on edificios for select using (opera_edificio(id));
create policy edificios_write on edificios for all using (
  tiene_permiso(org_propietaria_id, 'espacios')
  or (org_gestora_id is not null and tiene_permiso(org_gestora_id, 'espacios'))
);

create policy salas_select on salas for select using (opera_edificio(edificio_id));
create policy salas_write on salas for all using (
  exists (select 1 from edificios e where e.id = salas.edificio_id
    and (tiene_permiso(e.org_propietaria_id, 'espacios')
         or (e.org_gestora_id is not null and tiene_permiso(e.org_gestora_id, 'espacios'))))
);

create policy espacio_media_select on espacio_media for select using (
  (edificio_id is not null and opera_edificio(edificio_id))
  or (sala_id is not null and exists (select 1 from salas s where s.id = sala_id and opera_edificio(s.edificio_id)))
);
create policy espacio_media_write on espacio_media for all using (
  (edificio_id is not null and exists (select 1 from edificios e where e.id = edificio_id
     and (tiene_permiso(e.org_propietaria_id, 'espacios') or (e.org_gestora_id is not null and tiene_permiso(e.org_gestora_id, 'espacios')))))
  or (sala_id is not null and exists (select 1 from salas s join edificios e on e.id = s.edificio_id where s.id = sala_id
     and (tiene_permiso(e.org_propietaria_id, 'espacios') or (e.org_gestora_id is not null and tiene_permiso(e.org_gestora_id, 'espacios')))))
);

create policy planes_select on planes_reserva for select using (
  es_miembro(org_id)
  or exists (select 1 from edificios e where e.org_propietaria_id = planes_reserva.org_id and e.org_gestora_id is not null and es_miembro(e.org_gestora_id))
);
create policy planes_write on planes_reserva for all using (tiene_permiso(org_id, 'espacios'));

-- reservas: ver y crear, cualquier miembro que opere el edificio; cancelar propia o con permiso espacios
create policy reservas_select on reservas for select using (
  exists (select 1 from salas s where s.id = reservas.sala_id and opera_edificio(s.edificio_id))
);
create policy reservas_insert on reservas for insert with check (
  creada_por = auth.uid()
  and exists (select 1 from salas s where s.id = sala_id and opera_edificio(s.edificio_id))
);
create policy reservas_update on reservas for update using (
  creada_por = auth.uid()
  or exists (select 1 from salas s join edificios e on e.id = s.edificio_id where s.id = reservas.sala_id
       and (tiene_permiso(e.org_propietaria_id, 'espacios')
            or (e.org_gestora_id is not null and tiene_permiso(e.org_gestora_id, 'espacios'))))
);

create policy movimientos_select on movimientos for select using (tiene_permiso(org_id, 'finanzas'));
create policy movimientos_insert on movimientos for insert with check (
  tiene_permiso(org_id, 'finanzas') and origen = 'manual' and creado_por = auth.uid()
);
create policy movimientos_update on movimientos for update using (
  tiene_permiso(org_id, 'finanzas') and origen = 'manual'
);
```

- [ ] **Step 2: Aplicar**

Igual que la 0001: el controlador la aplica vía MCP (`apply_migration`) o CLI linkeada.
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add supabase && git commit -m "feat: migración 0002, RLS completa con helpers de membresía y co-gestión"
```

---

### Task 5: Seed de demo

**Files:**
- Create: `scripts/seed.mjs`
- Modify: `package.json` (script `"seed": "node --env-file=.env.local scripts/seed.mjs"`)

**Interfaces:**
- Consumes: `SUPABASE_SECRET_KEY` de `.env.local` (bypass RLS), esquema de Task 3-4 aplicado.
- Produces: usuarios `admin@demo.test` / `coordi@demo.test` / `ope@demo.test` (password `demo1234`), orgs "Fundación Delta" (propietaria) y "Gestora Sur" (gestora), edificio co-gestionado con reparto 60/40, salas, planes, 2 proyectos con tareas, clientes. `admin@demo.test` es super_admin y miembro de ambas orgs. **Idempotente**: correrlo dos veces no duplica nada.

- [ ] **Step 1: Escribir `scripts/seed.mjs`**

Node puro con `@supabase/supabase-js` y el secret key (`createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SECRET_KEY)`). Flujo:

1. Usuarios: por cada email, buscar con `auth.admin.listUsers()` (filtrar por email); si no existe, `auth.admin.createUser({ email, password: "demo1234", email_confirm: true, user_metadata: { name } })`. Nombres: "Ana Admin", "Camila Coordinadora", "Omar Operaciones". El trigger `crear_perfil` crea la fila en `perfiles`; hacer `update perfiles set nombre` por las dudas (upsert).
2. Datos (todo con `upsert`/select-antes-de-insertar para idempotencia, claves naturales = nombres):
   - Orgs "Fundación Delta" (tipo asociacion_civil) y "Gestora Sur" (tipo empresa).
   - Roles semilla en cada org — Administración `{"proyectos":true,"equipo":true,"finanzas":true,"espacios":true,"admin":true}`, Coordinación `{"proyectos":true,"equipo":true,"finanzas":false,"espacios":false,"admin":false}`, Operaciones `{"proyectos":true,"equipo":false,"finanzas":false,"espacios":true,"admin":false}`.
   - Membresías: admin→Administración en AMBAS orgs; coordi→Coordinación y ope→Operaciones en Fundación Delta.
   - `super_admins`: admin.
   - Edificio "Casa Delta" (propietaria=Fundación Delta, gestora=Gestora Sur, destino `reparto`, porcentaje_propietaria 60) con salas "Sala Norte" (publica), "Sala Sur" (publica), "Estudio" (privada).
   - Planes de Fundación Delta: "Gratuito" (gratuito=true, precio 0) y "Pago por hora" (gratuito=false, precio 8000).
   - Proyectos de Fundación Delta "Sitio nuevo" y "Campaña socios"; miembros: los 3 en "Sitio nuevo", admin+coordi en "Campaña socios"; 5 tareas en total (dificultades 1-5 variadas, 2 en pool con `asignado_a: null`, resto asignadas).
   - Clientes de Fundación Delta: "Estudio Sur", "Colectivo Raíz", "Marta Pérez" (con contactos de fantasía).
3. Al final, imprimir resumen: counts de orgs/usuarios/salas/planes/proyectos/tareas/clientes.

- [ ] **Step 2: Correr y verificar**

Run: `npm run seed` (dos veces: la segunda no debe duplicar).
Expected: counts estables — membresias 4, salas 3, roles 6, planes 2.

- [ ] **Step 3: Commit**

```bash
git add scripts/seed.mjs package.json && git commit -m "feat: seed de demo idempotente con dos orgs y edificio co-gestionado"
```

---

### Task 6: Auth — login, callback, middleware

**Files:**
- Create: `app/(auth)/ingresar/page.tsx`, `app/(auth)/ingresar/FormIngreso.tsx`, `app/auth/callback/route.ts`, `app/auth/salir/route.ts`, `middleware.ts`, `lib/supabase/middleware.ts`

**Interfaces:**
- Consumes: `crearClienteNavegador`, `crearClienteServidor` (Task 2).
- Produces: rutas `/ingresar` (pública), `/auth/callback`, `/auth/salir`; middleware que refresca sesión y redirige no-autenticados a `/ingresar`.

- [ ] **Step 1: `lib/supabase/middleware.ts`** — patrón estándar @supabase/ssr:

```ts
import { createServerClient } from "@supabase/ssr";
import { NextResponse, type NextRequest } from "next/server";

const RUTAS_PUBLICAS = ["/ingresar", "/auth"];

export async function actualizarSesion(request: NextRequest) {
  let respuesta = NextResponse.next({ request });
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll: () => request.cookies.getAll(),
        setAll: (all) => {
          all.forEach(({ name, value }) => request.cookies.set(name, value));
          respuesta = NextResponse.next({ request });
          all.forEach(({ name, value, options }) => respuesta.cookies.set(name, value, options));
        },
      },
    }
  );
  const { data: { user } } = await supabase.auth.getUser();
  const esPublica = RUTAS_PUBLICAS.some((r) => request.nextUrl.pathname.startsWith(r));
  if (!user && !esPublica) {
    const url = request.nextUrl.clone();
    url.pathname = "/ingresar";
    return NextResponse.redirect(url);
  }
  return respuesta;
}
```

`middleware.ts` en la raíz: exporta `middleware = actualizarSesion` con `config.matcher = ["/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|webp)$).*)"]`.

- [ ] **Step 2: Página `/ingresar`** — server component con logo texto "bigote" + `FormIngreso` (client). `FormIngreso`: botón "Continuá con Google" (`supabase.auth.signInWithOAuth({ provider: "google", options: { redirectTo: `${location.origin}/auth/callback` } })`), separador "o con tu email", input email + password con botón "Ingresá", y link "Mandame un enlace mágico" (`signInWithOtp({ email, options: { emailRedirectTo: `${location.origin}/auth/callback` } })` → estado "Revisá tu correo: te mandamos el enlace."). Errores en español ("No pudimos ingresarte. Revisá el email y la contraseña."). Estilo con tokens: fondo `bg-fondo`, form en `bg-superficie` borde `border-linea` redondeado 12px, botón primario `bg-acento text-acento-tinta` alto 44px.

- [ ] **Step 3: `app/auth/callback/route.ts`**

```ts
import { NextResponse } from "next/server";
import { crearClienteServidor } from "@/lib/supabase/server";

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url);
  const code = searchParams.get("code");
  if (code) {
    const supabase = await crearClienteServidor();
    await supabase.auth.exchangeCodeForSession(code);
  }
  return NextResponse.redirect(`${origin}/`);
}
```

`app/auth/salir/route.ts` (POST): `signOut()` + redirect a `/ingresar`.

- [ ] **Step 4: Google provider** — se configura en el Dashboard del proyecto hosted (Auth → Providers → Google) con un OAuth client de Google Cloud Console (redirect: `https://olmjkuapainklekdzntk.supabase.co/auth/v1/callback`). El implementador solo documenta esto en el README y deja el botón funcionando (si el provider no está configurado todavía, el botón muestra el error de Supabase en español: "Google no está configurado todavía."). También agregar `http://localhost:3000/auth/callback` a los Redirect URLs permitidos del Dashboard (Auth → URL Configuration) — documentarlo.

- [ ] **Step 5: Probar manualmente**

Run: `npm run dev` → `/ingresar` → login con `admin@demo.test` / `demo1234` → redirige a `/`. Magic link: probarlo con un email real si hay uno a mano (el SMTP built-in de Supabase tiene límite de ~2 emails/hora en free tier); si no, verificar solo que el submit muestra "Revisá tu correo".

- [ ] **Step 6: Build + commit**

```bash
npm run build && git add -A && git commit -m "feat: auth con Google, password y magic link"
```

---

### Task 7: Shell multi-org — contexto, switcher, navegación por permisos

**Files:**
- Create: `lib/org.ts`, `app/(app)/o/[orgId]/layout.tsx`, `app/(app)/o/[orgId]/page.tsx` (Hoy placeholder), `app/(app)/o/[orgId]/sin-acceso/page.tsx`, `app/page.tsx` (redirector raíz), `app/sin-organizacion/page.tsx`, `componentes/shell/BarraSuperior.tsx`, `componentes/shell/NavInferior.tsx`, `componentes/shell/SidebarEscritorio.tsx`, `componentes/shell/SwitcherOrg.tsx`

**Interfaces:**
- Consumes: `crearClienteServidor` (Task 2), tablas `membresias`/`roles`/`organizaciones` (Task 3-4).
- Produces: tipo `ContextoOrg = { org: { id: string; nombre: string }; permisos: Permisos; perfilId: string }` con `type Permisos = { proyectos: boolean; equipo: boolean; finanzas: boolean; espacios: boolean; admin: boolean }`; función `obtenerContextoOrg(orgId: string): Promise<ContextoOrg | null>` (null si no hay membresía activa); cookie `ultima_org` con el orgId visitado. Las fases siguientes cuelgan sus páginas bajo `app/(app)/o/[orgId]/`.

- [ ] **Step 1: `lib/org.ts`**

```ts
import { crearClienteServidor } from "@/lib/supabase/server";

export type Permisos = {
  proyectos: boolean; equipo: boolean; finanzas: boolean; espacios: boolean; admin: boolean;
};
export type ContextoOrg = {
  org: { id: string; nombre: string };
  permisos: Permisos;
  perfilId: string;
};

export async function obtenerContextoOrg(orgId: string): Promise<ContextoOrg | null> {
  const supabase = await crearClienteServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return null;
  const { data } = await supabase
    .from("membresias")
    .select("activo, roles(permisos), organizaciones(id, nombre)")
    .eq("org_id", orgId)
    .eq("perfil_id", user.id)
    .eq("activo", true)
    .maybeSingle();
  if (!data?.organizaciones) return null;
  const permisos = (data.roles as unknown as { permisos: Permisos }).permisos;
  const org = data.organizaciones as unknown as { id: string; nombre: string };
  return { org, permisos, perfilId: user.id };
}

export async function listarMisOrgs(): Promise<{ id: string; nombre: string }[]> {
  const supabase = await crearClienteServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return [];
  const { data } = await supabase
    .from("membresias")
    .select("organizaciones(id, nombre)")
    .eq("perfil_id", user.id)
    .eq("activo", true);
  return (data ?? []).map((m) => m.organizaciones as unknown as { id: string; nombre: string });
}
```

- [ ] **Step 2: Redirector raíz `app/page.tsx`** — server component: usuario no autenticado ya lo maneja el middleware; con sesión: leer cookie `ultima_org`; si tiene membresía activa ahí → `redirect(\`/o/\${orgId}\`)`; si no, `listarMisOrgs()`: 0 → `redirect("/sin-organizacion")`, ≥1 → redirect a la primera.

- [ ] **Step 3: `app/sin-organizacion/page.tsx`** — pantalla centrada: "Todavía no te invitaron a ninguna organización." + "Pedile la invitación a quien administra la tuya." + botón "Salir" (POST a `/auth/salir`). Si el usuario es super_admin (query a `super_admins`), mostrar link "Ir al panel de plataforma" → `/plataforma`.

- [ ] **Step 4: Layout de org `app/(app)/o/[orgId]/layout.tsx`** — `obtenerContextoOrg(params.orgId)`; null → redirect `/`; setear cookie `ultima_org` (via server action o en el redirector; las cookies no se escriben en layouts: hacerlo en `app/(app)/o/[orgId]/page.tsx` no alcanza — usar una route `app/(app)/o/[orgId]/tocar-cookie/route.ts` NO: simplificar, la cookie se setea desde `SwitcherOrg` (client) con `document.cookie = \`ultima_org=\${orgId}; path=/; max-age=31536000\`` en un `useEffect`). Estructura: `BarraSuperior` (mobile: SwitcherOrg + avatar/salir), `SidebarEscritorio` (desktop ≥1024: switcher arriba, nav vertical, salir abajo), `NavInferior` (mobile, fija abajo), `<main>` máx 1100px con padding 16/24.

- [ ] **Step 5: Navegación por permisos** — ítems: Hoy (`/o/[orgId]`, siempre), Tareas (`/o/[orgId]/tareas`, permiso `proyectos`), Espacios (`/o/[orgId]/espacios`, siempre — la disponibilidad es visible para todo miembro), Finanzas (`/o/[orgId]/finanzas`, permiso `finanzas`), Más (`/o/[orgId]/mas`, siempre). Ítems sin permiso NO se renderizan. Íconos Lucide 20px: `Sun`, `ListChecks`, `DoorOpen`, `Wallet`, `Menu`. Ítem activo: texto/ícono `text-acento`. `NavInferior`: alto 64px, fondo `bg-panel`, borde superior `border-linea`, targets ≥44px.

- [ ] **Step 6: `SwitcherOrg.tsx`** (client) — muestra nombre de la org actual + chevron; tap → popover (lista de `listarMisOrgs()` pasada como prop desde el layout) → click navega a `/o/<id>` con `router.push`. Con una sola org: solo el nombre, sin popover. Setea cookie `ultima_org` al montar.

- [ ] **Step 7: Páginas placeholder** — `page.tsx` (Hoy): saludo "Hola, {nombre}" + secciones vacías que enseñan ("Acá van a aparecer tus tareas y reservas del día."). `sin-acceso/page.tsx`: "No tenés acceso a esta sección." + link a Hoy. Crear también `tareas/page.tsx`, `espacios/page.tsx`, `finanzas/page.tsx`, `mas/page.tsx` como placeholders de una línea (guardia: cada página con permiso requerido llama `obtenerContextoOrg` y redirige a `../sin-acceso` si falta el permiso).

- [ ] **Step 8: Probar los tres roles**

Run: `npm run dev`. `admin@demo.test` → ve todo + switcher con 2 orgs y cambia entre ellas; `coordi@demo.test` → sin Finanzas en la nav; `ope@demo.test` → sin Finanzas; URL directa `/o/<org>/finanzas` como coordi → "No tenés acceso".

- [ ] **Step 9: Build + commit**

```bash
npm run build && npm run lint
git add -A && git commit -m "feat: shell multi-org con switcher y navegación por permisos"
```

---

### Task 8: Tests de RLS (Vitest)

**Files:**
- Create: `vitest.config.ts`, `tests/rls/helpers.ts`, `tests/rls/aislamiento.test.ts`, `tests/rls/reservas.test.ts`
- Modify: `package.json` (script `"test": "vitest run"`)

**Interfaces:**
- Consumes: proyecto hosted con migraciones aplicadas y seed corrido (`npm run seed`); `.env.local`.
- Produces: `clienteComo(email: string): Promise<SupabaseClient>` — cliente autenticado con password `demo1234`; `clienteAdmin(): SupabaseClient` (secret key, para limpieza).

- [ ] **Step 1: Setup**

```bash
npm install -D vitest dotenv
```

`vitest.config.ts`: `test: { environment: "node", testTimeout: 20000, setupFiles: ["tests/setup.ts"] }`; `tests/setup.ts`: `import { config } from "dotenv"; config({ path: ".env.local" });`. `tests/rls/helpers.ts`:

```ts
import { createClient, type SupabaseClient } from "@supabase/supabase-js";

const URL = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const ANON = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!;

export function clienteAdmin(): SupabaseClient {
  return createClient(URL, process.env.SUPABASE_SECRET_KEY!, {
    auth: { persistSession: false },
  });
}

export async function clienteComo(email: string): Promise<SupabaseClient> {
  const cliente = createClient(URL, ANON, { auth: { persistSession: false } });
  const { error } = await cliente.auth.signInWithPassword({ email, password: "demo1234" });
  if (error) throw new Error(`No pude ingresar como ${email}: ${error.message}`);
  return cliente;
}
```

- [ ] **Step 2: Escribir tests (fallan si la RLS está mal)**

`tests/rls/aislamiento.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { clienteComo } from "./helpers";

describe("aislamiento entre organizaciones", () => {
  it("coordi (solo Fundación Delta) no ve datos de Gestora Sur", async () => {
    const coordi = await clienteComo("coordi@demo.test");
    const { data: orgs } = await coordi.from("organizaciones").select("nombre");
    const nombres = (orgs ?? []).map((o) => o.nombre);
    expect(nombres).toContain("Fundación Delta");
    expect(nombres).not.toContain("Gestora Sur");
  });

  it("coordi sin permiso finanzas no lee movimientos", async () => {
    const coordi = await clienteComo("coordi@demo.test");
    const { data } = await coordi.from("movimientos").select("id");
    expect(data).toEqual([]);
  });

  it("coordi sin permiso espacios no puede crear salas", async () => {
    const coordi = await clienteComo("coordi@demo.test");
    const { data: salas } = await coordi.from("salas").select("edificio_id").limit(1);
    const { error } = await coordi
      .from("salas")
      .insert({ edificio_id: salas![0].edificio_id, nombre: "Intrusa" });
    expect(error).not.toBeNull();
  });

  it("admin de ambas orgs ve las dos", async () => {
    const admin = await clienteComo("admin@demo.test");
    const { data } = await admin.from("organizaciones").select("nombre");
    expect((data ?? []).length).toBeGreaterThanOrEqual(2);
  });
});
```

`tests/rls/reservas.test.ts`: (a) `ope@demo.test` (permiso espacios en Fundación Delta) crea reserva para sí en Sala Norte → OK; (b) reserva solapada misma sala/fecha/franja → error con código `23P01` (exclusion); (c) `coordi` (sin espacios) crea reserva para sí → OK (cualquier miembro reserva), pero update de una reserva ajena → 0 filas afectadas; (d) reserva con `cliente_id` y `para_perfil_id` ambos seteados → error de check. Usar fechas distintas por test (`2027-08-0X`). **Limpieza**: en `afterAll`, borrar con `clienteAdmin()` todas las reservas creadas por los tests (delete por fechas `2027-08-*`) — la DB hosted persiste entre corridas y los tests deben poder correrse dos veces seguidas.

- [ ] **Step 3: Correr**

Run: `npm test` (dos veces seguidas: ambas verdes — prueba la limpieza).
Expected: todos verdes. Si alguno falla por política mal escrita, avisar al controlador: el fix va como migración nueva `000X_fix_rls.sql` aplicada vía MCP (la DB hosted ya tiene 0002 aplicada; no editar migraciones ya aplicadas).

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "test: RLS de aislamiento entre orgs, permisos y reservas"
```

---

### Task 9: Panel plataforma (super-admin)

**Files:**
- Create: `app/plataforma/page.tsx`, `app/plataforma/acciones.ts` (server actions), `supabase/migrations/0003_crear_org.sql`

**Interfaces:**
- Consumes: `crearClienteAdmin` (Task 2), `es_super_admin()` (Task 4).
- Produces: función SQL `crear_organizacion(nombre text, tipo text, email_admin text) returns uuid` (security definer, chequea `es_super_admin()`); página `/plataforma`.

- [ ] **Step 1: Migración 0003** — función que: valida `es_super_admin()` (si no, `raise exception 'Solo plataforma'`); crea la org; crea los 3 roles semilla (mismos jsonb que el seed de Task 5); devuelve el id. La membresía del primer admin la crea la server action (necesita invitar al usuario primero).

- [ ] **Step 2: `acciones.ts`** — server action `crearOrgConAdmin(formData)`: verifica super-admin con el cliente de sesión (`select` a `super_admins`); llama `rpc("crear_organizacion", ...)`; con `crearClienteAdmin()`: `auth.admin.inviteUserByEmail(email)` (si ya existe, `listUsers` y tomar el id); inserta membresía con rol Administración de la org nueva. `revalidatePath("/plataforma")`.

- [ ] **Step 3: Página `/plataforma`** — server component: si no es super_admin → redirect `/`. Lista de organizaciones (nombre, tipo, cantidad de miembros) + form: nombre, tipo (select empresa/asociación civil/otro), email del primer admin, botón "Creá la organización". Sin diseño especial: tokens base, una columna.

- [ ] **Step 4: Probar** — como `admin@demo.test` crear "Org Prueba" con un email nuevo → aparece en la lista con sus 3 roles creados y la membresía del invitado. Como `coordi` → `/plataforma` redirige. (El email de invitación real depende del SMTP de Supabase; verificar la membresía y roles por DB, no por el mail.)

- [ ] **Step 5: Build + test + commit**

```bash
npm run build && npm test
git add -A && git commit -m "feat: panel de plataforma para crear organizaciones"
```

---

### Task 10: README + cierre de fase

**Files:**
- Modify: `README.md`

- [ ] **Step 1: README completo** — qué es bigote, stack, setup paso a paso (proyecto Supabase hosted, `.env.local` desde `.env.example` con publishable + secret key del Dashboard, aplicar migraciones con `npx supabase link --project-ref <ref> && npx supabase db push` o MCP, `npm run seed`, `npm run dev`, usuarios demo con password `demo1234`), cómo correr tests, variables para deploy en Vercel, configuración de Google OAuth y Redirect URLs, estructura de carpetas.

- [ ] **Step 2: Verificación final de fase**

Run: `npm run build && npm run lint && npm run seed && npm test`
Expected: todo verde. Probar en el navegador con viewport 380px que la nav inferior y el login se usan bien.

- [ ] **Step 3: Commit + push**

```bash
git add -A && git commit -m "docs: README con setup local y deploy" && git push
```

---

## Self-review (hecho al escribir)

- **Cobertura spec fase 1:** esquema §3 completo (Task 3), RLS §4 (Task 4), seeds §8 (Task 5), auth §5.1 (Task 6), shell/switcher/nav §5.1 (Task 7), tests RLS §8 (Task 8), plataforma §5.5 (Task 9), README §8 (Task 10). El trigger reserva→movimientos es fase 4 (spec §7); el constraint anti-solape sí queda acá.
- **Sin placeholders:** el único código diferido a criterio del implementador está en seeds y páginas placeholder, con contenido y valores especificados.
- **Consistencia de nombres:** `crearClienteNavegador/Servidor/Admin`, `obtenerContextoOrg`, `listarMisOrgs`, `es_miembro`, `tiene_permiso`, `opera_edificio`, cookie `ultima_org` — usados igual en todas las tareas.
